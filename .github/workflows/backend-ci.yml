name: Backend CI

on:
  push:
    branches: [ "main", "develop" ]
    paths:
      - 'user-service/**'
      - 'operator-service/**'
      - 'recharge-service/**'
      - 'payment-service/**'
      - 'notification-service/**'
      - 'api-gateway/**'
      - 'eureka-server/**'
      - 'config-server/**'
      - 'pom.xml'
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        service: [
          'user-service',
          'operator-service',
          'recharge-service',
          'payment-service',
          'notification-service',
          'api-gateway',
          'eureka-server',
          'config-server'
        ]

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Build and Test ${{ matrix.service }}
      run: mvn -B test --file ${{ matrix.service }}/pom.xml

    # Uncomment and configure SONAR_TOKEN secret to enable SonarQube analysis
    - name: SonarQube Scan
      if: success() && matrix.service != 'eureka-server' && matrix.service != 'config-server'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=omnicharge-${{ matrix.service }} --file ${{ matrix.service }}/pom.xml
